# abort script on any command that exits with a non zero value
set -e
cd mongod
tar xfz mongo-r3.0.14.tar.gz
cd mongo-r3.0.14
/var/vcap/packages/scons/bin/scons mongod --disable-warnings-as-errors

cp build/linux2/normal/mongo/mongod ${BOSH_INSTALL_TARGET}